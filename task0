#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/**
 * main - calculate the power of a number with pow function .
 *
 * Return: Always 0 (Success)
 */
main()
{
float base=2, expo=2, result;
result = pow(base, expo);
return (0);
}
